# Base URL for the API. Leave unset for local dev (Vite proxy handles it).
# In production, set this to your deployed API's /api path if serving
# the frontend and backend separately.
VITE_API_URL=/api
